extends layout

block content
  h1= title
  p Welcome to #{title}
  p Added a text

   br
  - var btnType = 'danger'
  - var btnSize = 'sm'
  button(type='button' class='btn btn-' + btnType + ' btn-' + btnSize) Hit me
